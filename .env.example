# Copy to .env and replace values with real secrets.
# Never commit .env.

ADMIN_TOKEN=replace-with-a-long-random-secret
POSTGRES_DB=temperature_logger
POSTGRES_USER=logger
POSTGRES_PASSWORD=replace-with-a-strong-password
DATABASE_URL=postgres://logger:replace-with-a-strong-password@postgres:5432/temperature_logger
ALLOWED_ORIGINS=http://localhost:3000,https://debeerswang.github.io
